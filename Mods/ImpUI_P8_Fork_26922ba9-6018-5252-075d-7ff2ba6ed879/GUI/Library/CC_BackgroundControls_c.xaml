<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
					xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
					xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
					xmlns:System="clr-namespace:System;assembly=mscorlib"
					xmlns:ls="clr-namespace:ls;assembly=Code"
					xmlns:noesis="clr-namespace:NoesisGUIExtensions;assembly=Noesis.GUI.Extensions"
					mc:Ignorable="d">

      <ControlTemplate x:Key="backgroundSelectionTemplate">
        <DockPanel>
          <StackPanel Width="{StaticResource gameplayPanelWidth}" HorizontalAlignment="Left" DockPanel.Dock="Top">
            <Image Source="{StaticResource CarouselDecoration}" Stretch="None" Margin="25,98,0,-148" Height="108" HorizontalAlignment="Center">
                <Image.RenderTransform>
                    <ScaleTransform ScaleY="-1"></ScaleTransform>
                </Image.RenderTransform>
            </Image>
            <TextBlock ls:TextBlockFormatter.SourceText="{Binding SelectedBackground.Name}" Style="{StaticResource PanelHeaderText}" TextAlignment="Center"/>
        </StackPanel>

        <StackPanel x:Name="selectedBackgroundDescription" Orientation="Horizontal" DockPanel.Dock="Top">
            <ScrollViewer Style="{StaticResource gameplayPanelScrollViewerStyle}"
                          VerticalScrollBarVisibility="Auto" MaxHeight="800" Height="Auto"
                          DockPanel.Dock="Top" Margin="0,20,0,0">
                <ListBox x:Name="backgroundListBox" ItemTemplate="{StaticResource CustomIconTemplate}"
                        ItemsSource="{Binding SelectableBackgrounds}" SelectedItem="{Binding SelectedBackground}"
                        ItemContainerStyle="{StaticResource CCIconTextListBoxItemStyle}" Style="{StaticResource gameplayIconGridStyle}">
                    <ListBox.Resources>
                        <Style x:Key="CustomIconStyle" BasedOn="{StaticResource BackgroundIconStyle}" TargetType="Rectangle"/>
                    </ListBox.Resources>
                </ListBox>
            </ScrollViewer>
        </StackPanel>
        </DockPanel>
    </ControlTemplate>

    <ControlTemplate x:Key="backgroundDescriptionTemplate">
          <StackPanel Width="{StaticResource gameplayPanelWidth}" HorizontalAlignment="Left" DockPanel.Dock="Top">
              <ContentControl Template="{StaticResource PanelSideDescription}" VerticalAlignment="Top">
                  <TextBlock ls:TextBlockFormatter.SourceText="{Binding SelectedBackground.Description}" Style="{StaticResource CarouselSideDescriptionText}"/>
              </ContentControl>
          </StackPanel>
    </ControlTemplate>

    <ControlTemplate x:Key="backgroundProgressionsTemplate">
        <Grid HorizontalAlignment="Left">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <Image Source="{StaticResource CarouselDecoration}" Stretch="None" Margin="0,-30,0,0" Height="108" HorizontalAlignment="Center" />
                <TextBlock ls:TextBlockFormatter.SourceText="{Binding Source='h230f60c6g161fg481bgbe52g42bc264618dd', Converter={StaticResource TranslatedStringConverter}}" Style="{StaticResource FeatureListHeaderText}"/>
            </StackPanel>
            <ScrollViewer Style="{StaticResource gameplayPanelScrollViewerStyle}" VerticalScrollBarVisibility="Auto"  Grid.Row="1" Margin="0,-20,0,0">
                <StackPanel Style="{StaticResource gameplayScrolledStackPanelStyle}" VerticalAlignment="Stretch">
                  <Control Template="{StaticResource progressionFeaturesList}" DataContext="{Binding ProgressionData.BackgroundProgression}" HorizontalAlignment="Center"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </ControlTemplate>
</ResourceDictionary>